export interface BaseStudentLevelFormValues {
    studentId: string;
    sellerId: string;
    moduleIds: string[];
}

export interface BaseUpdateStudentLevelFormValues {
    contractId: string;
    studentId: string;
    status: string;
}
